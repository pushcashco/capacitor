{
  "name": "push-apple-pay-example",
  "version": "0.0.1",
  "private": true,
  "description": "Example app for the Push Cash Apple Pay Capacitor SDK",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "sync": "npm run build && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@pushcashco/capacitor": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "vite": "^5.4.0"
  }
}
